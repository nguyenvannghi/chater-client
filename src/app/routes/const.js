const rHome = '/home';
const rChater = '/chater';
const rAbout = '/about';
const rLogin = '/sign-in';

const RouterApp = {
    rChater,
    rHome,
    rAbout,
};

const RouterNoAuth = {
    rLogin,
};

export { RouterApp, RouterNoAuth };
